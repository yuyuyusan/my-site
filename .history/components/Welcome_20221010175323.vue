<template>
  <div>
    <div class="loading">
      <div class="loadingBg">
        <div class="circle"></div>
        <p class="text">Loading</p>
      </div>
    </div>
    <div class="loadingAfter">
      <p class="loadingAfter__text">WELCOME!</p>
    </div>
  </div>
</template>

<script>
export default {
}
</script>
<style lang="scss" scoped>
// ロード中


.loading {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 2;
}

.loadingBg {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: #000;
  display: flex;
  justify-content: center;
  align-items: center;
  transition: 0.5s;
}

.loadingBg .circle {
  width: 30px;
  height: 30px;
  border: 1px solid #fff;
  border-top: 1px solid #000;
  border-radius: 50%;
  animation: 3s linear loadingCircle infinite;
}

@keyframes loadingCircle {
  0% {
    transform: rotate(0);
  }

  100% {
    transform: rotate(360deg);
  }
}

.loadingBg .text {
  position: absolute;
  top: calc(50% + 50px);
  left: 50%;
  animation: 1.3s loadingText infinite;
  transform: translate(-50%, -50%);
  z-index: 1;
  font-size: 3.2rem;
  font-family: $f-roboto;
  color: #fff;
}

@keyframes loadingText {
  50% {
    top: calc(50% + 40px);
    transform: translate(-50%, -50%) scale(.8);
  }
}

// ロード画面完了後
.loaded {
  z-index: -1;
}

.loadingBgHidden {
  animation: loadingBgHidden 1s ease-in forwards;
}

@keyframes loadingBgHidden {
  40% {
    transform: translateY(0%);
  }

  100% {
    transform: translateY(-100%);
    display: none;
  }
}

// ロード完了後
.loadingAfter {
  width: 100%;
  height: 100%;
  display: grid;
  place-content: center;
  position: fixed;
  background: #000;
  top: 0;
  left: 0;
  z-index: 1;
}

.loadingAfter__hidden {
  animation: loadingAfterText__bg 1s ease-in forwards;
}

@keyframes loadingAfterText__bg {
  60% {
    transform: translateY(0%);
  }

  100% {
    transform: translateY(-100%);
    display: none;
  }
}

.loadingAfter__text {
  width: 0;
  margin: 0;
  font-size: 5rem;
  font-family: $f-roboto;
  font-weight: 700;
  color: #fff;
  white-space: nowrap;
  overflow: hidden;
}

.loadedText {
  animation: flowing-anim .5s forwards linear;
}

@keyframes flowing-anim {
  0% {
    width: 0;
  }

  100% {
    width: 100%;
  }
}
</style>

